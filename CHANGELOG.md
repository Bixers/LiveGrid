# Changelog

## 0.1.8 - 2026-09-04

- 修复后端刷新生成新签名参数时，同一直播流被反复执行 `loadfile replace` 的问题。
- 原生播放器不再把主动替换产生的 `MPV_END_FILE_REASON_STOP` 误判为断流并重复重试。
- Windows 嵌入播放改用 `gpu + d3d11 + d3d11va-copy`，提升黑屏场景下的兼容性。
- 仅在真实中断后使用后端返回的新签名恢复播放，并记录 mpv 结束原因及警告日志。

## 0.1.7 - 2026-09-04

- 修复隐藏调度窗口未创建 HWND，导致 libmpv 播放和原生弹幕命令全部未执行的问题。
- 修复底部播放控制条的画质选择器尺寸、配色和箭头定位。
- 桌面日志增加原生播放器就绪、错误和输入管道状态，便于定位本地运行时故障。

## 0.1.6 - 2026-09-04

- 播放路线切换为 libmpv，每个房间持有独立播放器、弹幕连接和运行状态。
- 取流任务限制为最多 2 个并发；房间状态刷新加入随机错峰和指数退避。
- 弹幕连接轮换 `8501` 至 `8506`，按 `1/2/4/8/15/15` 秒重试，并加入 10 秒连接/握手超时和 45 秒心跳。
- 默认只保留单房间音频焦点，五路以上自动尝试把非主画面切换为标清。
- 移除贵宾数功能，房间检查器拆分为独立弹幕栏和礼物栏，并支持排除免费礼物。
- 底部播放条增加清晰度控制，顶栏离开顶部区域后收起，避免遮挡视频。

## 0.1.5 - 2026-09-04

- 直播窗口顶栏改为鼠标进入或键盘聚焦时显示，隐藏时不再占用直播画面高度。
- 在直播窗口顶栏显示实时贵宾数，并随运行数据自动刷新。
- 新增逐房间音量滑杆，支持播放条与检查器同步控制和保存。
- 桌面偏好改为固定用户目录存储，修复关注、音量及布局在重启后丢失的问题。
- 启用 FLV/HLS 直播低延迟同步、缓冲追赶和视频帧停滞恢复，降低长时间播放后的音画不同步。
- 贵宾数改为订阅 `/api/events` 与弹幕通道的实时事件，实时值优先于统计快照。
- 弹幕桥接兼容斗鱼新版 `oni` 的 `vn/un` 字段，修复实时贵宾数始终为 0。
- 礼物事件兼容后端 `sender/giftCount` 字段，修复送礼用户和数量显示不准确。
- 直播顶栏仅在鼠标位于顶部控制区域时显示，移出控制区域立即隐藏。
- 直播顶栏新增礼物收入，单位为 CNY，可在显示设置中切换当日、近 7 日和本次直播。

## 0.1.4 - 2026-09-03

- 自由窗口改为独立开关；关闭后锁定窗口并使用自动、1/2/3/4 列或重点布局。
- 修复直播卡片关闭、声音和弹幕按钮点击无效及状态反馈不清的问题。
- 使用紧凑的自定义直播控制条替代 Chromium 原生进度控制条。
- 新增弹幕透明度、字号和显示区域设置，支持顶部 1/4、顶部 1/3、上半屏、全屏、下半屏和底部 1/4。
- 自由窗口位置与尺寸不再被预设布局覆盖。

## 0.1.3 - 2026-09-03

- 恢复原版 FLV/HLS 播放策略，健康流持续播放，仅在卡顿、失败或明确刷新后切换地址。
- 恢复共享 SSE 弹幕连接和每个直播窗口上的滚动弹幕浮层。
- 直播窗口恢复自由拖动、八方向缩放、层级置顶与位置尺寸保存。
- 房间队列和房间检查器均可隐藏，让直播画布占满工作区。
- 不再复用旧版残留的 `DouyuViewer.exe`，退出时结束完整后端进程树。
- 后端 PyInstaller 临时目录跟随安装盘，安装器直接释放文件到目标目录。

## 0.1.2 - 2026-09-03

- 将英文及技术名称统一为 `LiveGrid`，中文产品名统一为“监控室”。
- 重做桌面工作台的信息层级、直播网格、队列和事件检查器界面。
- 移除旧产品品牌、账户等级、无关外链和远程公共聊天入口。
- 调整 NSIS 安装目录逻辑，选择磁盘或父目录时自动创建“监控室”子目录。
- 改为标准安装版优先，避免便携版启动时重复释放 Electron 运行时。
- 增加窗口显示兜底、无响应和渲染进程异常日志。
- 增加房间筛选、批量操作、布局切换、拖动排序、清晰度与静音控制。
